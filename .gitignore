# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / coverage / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# --- Project caches & generated data ---
# Embedding cache written by name_embeddings.py (EmbeddingCache, cache_dir=".emb_cache")
.emb_cache/

# Model weights / checkpoints (wojood.py, name_model.py)
*.pt
*.pth
*.ckpt
*.bin
*.safetensors
models/
checkpoints/

# Hugging Face / torch download caches, if pointed at the repo
.cache/
.hf_cache/

# --- Secrets & local config ---
.env
.env.*
!.env.example
*.key
secrets.json

# --- Logs & temp files ---
*.log
logs/
tmp/
temp/
*.tmp
*.bak
*.orig
*.rej

# --- Editors / IDEs ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- OS cruft ---
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# --- Notes ---
# Corpus files (kafi*.txt, khoei.txt) and their derived outputs
# (*.hadiths.json, *.chains.txt, *.clean.graph.json/.dot, *.rejected.json)
# are intentionally tracked -- do not add them here.
